# mysql-check-tools
Upload the SQL file, parse it, and perform data verification on the database tables. Additionally, it supports converting the SQL file into a dict object, and saving the dict in JSON or YAML format
